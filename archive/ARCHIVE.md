# Archive — registro de disposiciones

Este directorio es el destino de todo modulo que deja de ser alcanzable desde
un entry point real (un `run:` de `.github/workflows/*.yml` o la suite de
tests). La regla del proyecto es **archivar, nunca borrar**: un modulo que hoy
parece muerto puede ser la unica documentacion de como se cargo una temporada
historica, y un `git rm` convierte esa informacion en arqueologia.

`archive/` esta excluido del recorrido del auditor
(`tools/audit/graph.py::EXCLUDED_DIRS`), asi que el codigo retirado no puede
volver a disparar hallazgos de `broken-reference`, `orphan-code` ni
`circular-dependency`. Esa exclusion es justo lo que hace que archivar sea una
disposicion valida y no un barrido bajo la alfombra: la fila de abajo es la
que rinde cuentas.

## Registro

| Module | Archived on | Reason | Disposition | Safe to re-run? |
|--------|-------------|--------|-------------|-----------------|
| `load_10_seasons.py` | 2026-09-03 | Su primera linea es `from src.data.collector import download_season` y `src/data/collector.py` no existe en ninguna revision del repositorio: `src/data/` solo contiene `queries.py`. El script revienta con ImportError antes de ejecutar una sola instruccion, de modo que era una referencia rota permanente (S1) en la raiz del proyecto. | Movido sin modificar a `archive/load_10_seasons.py`. El blob es identico al original (`27892b1`), no se reescribio ni una linea. La carga historica de temporadas la cubre hoy `scripts/load_historical_data.py`, que si tiene su collector real y corre desde el modo `weekly` del orchestrator. | **No — nunca corrio.** No es un one-shot ya aplicado: al fallar en el import jamas escribio una fila en `matches` ni gasto un credito de API. Re-ejecutarlo tal cual es imposible (mismo ImportError); reconstruir `src.data.collector` para revivirlo duplicaria la carga historica que ya hace `load_historical_data.py` y meteria partidos repetidos. Usar ese script en su lugar. |
| `src/betting/probabilities.py` | 2026-09-04 | Prototipo temprano: `market_probabilities(matrix)` recorre la matriz de marcadores con dos bucles anidados y acumula home/draw/away, over25 y btts a mano. Cero referencias a `src.betting` en todo el arbol: ningun workflow ni test lo alcanza. Las probabilidades de produccion salen del ensemble (Dixon-Coles + Poisson + ELO + xG + H2H + forma), no de un recuento manual. | Movido sin modificar a `archive/src/betting/probabilities.py`; el blob es identico al original (`de9d68b`). Lo supersede `src/models/poisson_markets.py`, que deriva los mismos mercados desde la matriz ya corregida por Dixon-Coles. | **Si — funcion pura.** No toca DB, red ni disco. Pero revivirlo seria un retroceso: ignora la correccion tau de Dixon-Coles y no pasa por `apply_calibration()`, asi que devolveria probabilidades sin calibrar. |
| `src/betting/value_bets.py` | 2026-09-04 | Cuatro lineas: `find_value(prob, odds)` devuelve `prob - 1/odds > 0.05`. El umbral 0.05 esta hardcodeado y es el mismo para todos los mercados, lo que contradice de frente la regla de `MIN_EDGE_BY_MARKET`, donde cada mercado tiene su propio minimo. Sin importadores. | Movido sin modificar a `archive/src/betting/value_bets.py` (blob `76c3296`). Lo superseden `src/models/value_bet.py` y el filtro por `edge_market` con el lookup `_min_edge` de `src/pipeline/prediction_pipeline.py`. | **Si — funcion pura, pero no reintroducir.** Acierta al usar `edge_market` (`prob - 1/odds`) y no `edge_ev`, pero su umbral plano de 5% dejaria pasar mercados que hoy exigen mas edge; volver a llamarlo abriria apuestas que el filtro por mercado rechaza. |
| `src/features/team_mapping_engine.py` | 2026-09-04 | Motor de mapeo alterno al vigente: `load_mapping()` y `save_mapping()` leen y escriben la tabla `team_name_mapping`, que **no existe** — ningun DDL del repositorio la crea y el nombre solo aparece dentro de este mismo archivo. Ademas `match_team()` escribe en DB desde una ruta de matching y reporta con `print()` en vez de log. Sin importadores. | Movido sin modificar a `archive/src/features/team_mapping_engine.py` (blob `d4bb64b`). Lo supersede `src/utils/team_normalizer.py::normalize_team()`, que resuelve alias en codigo (`team_alias_map.py`, `team_name_map.py`, `national_team_aliases.py`) y no necesita una tabla de mapeo persistida. | **No — revienta contra la DB.** `load_mapping()` hace un SELECT sobre `team_name_mapping`, una tabla inexistente: ProgrammingError inmediato, antes de mapear nada. Y si alguien creara la tabla para revivirlo, `save_mapping()` empezaria a persistir automatches con score >= 85 sin revision humana, compitiendo con el alias map vigente. |
| `src/features/team_matcher.py` | 2026-09-04 | `match_team_name()` hace fuzzy matching con rapidfuzz (threshold 85) y, cuando no alcanza el umbral, devuelve el nombre crudo de la API sin normalizar. Eso viola la regla de que todo nombre pase por `normalize_team()` antes de consultar la DB: `matches` y `upcoming_matches` guardan minusculas normalizadas, asi que un nombre crudo no matchea nunca y deja bets en `pending` para siempre. Sin importadores. | Movido sin modificar a `archive/src/features/team_matcher.py` (blob `bc24980`). Lo supersede `src/utils/team_normalizer.py::normalize_team()`, punto de entrada unico para normalizar nombres antes de cualquier query. | **No — leeria la DB con el criterio equivocado.** Su `get_db_teams()` consulta `upcoming_matches` sin el `DISTINCT ON (home_team_norm, away_team_norm, sport_key) ORDER BY updated_at DESC` obligatorio, de modo que arrastraria los duplicados stale que esa tabla acumula y alimentaria el matcher con nombres viejos. |
| `src/markets/asian_handicap.py` | 2026-09-04 | Archivo vacio: 0 bytes (blob `e69de29`, el hash del blob vacio de git). Placeholder que nunca se escribio, pese a que el handicap asiatico si es un mercado activo en produccion — la carpeta `src/markets/` aparentaba cubrirlo y no cubria nada. | Movido tal cual a `archive/src/markets/asian_handicap.py`. Lo supersede `src/models/asian_handicap_model.py`, que ademas resuelve las claves parametrizadas (`ah_home_-0.5`) hacia los grupos `ah_home_fav` / `ah_home_pk` / `ah_home_dog` via `_ah_group()`. | **No aplica — no hay codigo que ejecutar.** Importarlo no falla, pero no define ningun simbolo: cualquier `from ... import` sobre el revienta con ImportError. |
| `src/markets/btts.py` | 2026-09-04 | `compute_btts(matrix)` suma en nueve lineas la probabilidad de que ambos equipos anoten recorriendo la matriz de marcadores. Sin importadores: ninguna referencia a `src.markets` en el arbol. | Movido sin modificar a `archive/src/markets/btts.py` (blob `4c9fa87`). Lo supersede `src/models/poisson_markets.py`, que calcula BTTS sobre la matriz corregida por Dixon-Coles y luego pasa por calibracion. | **Si — funcion pura.** Sin efectos secundarios, pero su salida no esta calibrada ni corregida por tau, asi que reintroducirla degradaria las probabilidades que hoy alimentan el filtro de edge. |
| `src/markets/correct_score.py` | 2026-09-04 | `best_scores(matrix)` devuelve los marcadores mas probables leyendo la matriz. Once lineas, sin importadores. El marcador exacto ni siquiera es un mercado que el pipeline apueste hoy. | Movido sin modificar a `archive/src/markets/correct_score.py` (blob `9ef2a5a`). La derivacion de mercados desde la matriz vive en `src/models/poisson_markets.py`; correct score no forma parte de los mercados activos. | **Si — funcion pura.** No toca DB ni red. Reactivar el mercado exigiria antes calibracion propia y una entrada en `MIN_EDGE_BY_MARKET`, que hoy no existen. |
| `src/markets/market_1x2.py` | 2026-09-04 | `compute_1x2(matrix)` agrega la matriz en home/draw/away con numpy. Nueve lineas, sin importadores, duplicando lo que el modelo vigente ya entrega. | Movido sin modificar a `archive/src/markets/market_1x2.py` (blob `54753d6`). Lo superseden `src/models/dixon_coles_model.py` y `src/models/poisson_markets.py`, que producen 1X2 con la correccion de marcadores bajos. | **Si — funcion pura.** Sin efectos secundarios, pero asume una matriz de Poisson independiente; usada sobre la matriz de produccion daria un 1X2 inconsistente con el resto del pipeline. |
| `src/markets/over_under.py` | 2026-09-04 | `compute_over_under(matrix)` acumula la probabilidad de superar una linea de goles recorriendo la matriz. Diez lineas, sin importadores. | Movido sin modificar a `archive/src/markets/over_under.py` (blob `2ffeeb0`). Lo supersede `src/models/poisson_markets.py`, que genera las lineas over/under vigentes. | **Si — funcion pura.** Sin efectos secundarios, pero no aplica calibracion por mercado ni por liga, de modo que sus probabilidades no son comparables con las que filtra `edge_market`. |
| `src/probability/poisson_model.py` | 2026-09-04 | Archivo vacio: 0 bytes (blob `e69de29`). Placeholder que nunca se escribio y que sin embargo hacia parecer que `src/probability/` era un paquete de modelado vigente. | Movido tal cual a `archive/src/probability/poisson_model.py`. El modelado Poisson real vive en `src/models/poisson_markets.py` y `src/models/dixon_coles_model.py`. | **No aplica — no hay codigo que ejecutar.** Importarlo no falla, pero no define nada; cualquier `from ... import` sobre el revienta con ImportError. |
| `src/probability/score_matrix.py` | 2026-09-04 | `build_score_matrix(home_lambda, away_lambda, max_goals=7)` construye la matriz como el producto de dos Poisson independientes. Esa independencia es exactamente lo que Dixon-Coles corrige: la dependencia en marcadores bajos (0-0, 1-0, 0-1, 1-1). Sin importadores. | Movido sin modificar a `archive/src/probability/score_matrix.py` (blob `ee0c49a`). Lo supersede `src/models/dixon_coles_model.py`, que aplica el factor tau sobre esos mismos marcadores. | **Si — funcion pura (numpy + scipy).** Sin efectos secundarios, pero sus probabilidades estan sesgadas justo en los marcadores bajos, que son los que mas pesan en 1X2 y BTTS; usar la matriz de Dixon-Coles. |

## Conservados a proposito (no archivados)

La regla es que **ningun modulo queda sin clasificar**: si el auditor lo
reporta en `orphan-code` y no se movio a `archive/`, tiene que estar aqui con
el motivo por el que se queda. Las cuatro filas de abajo no son codigo muerto
que toleramos, son dos falsos positivos estructurales del algoritmo de
alcanzabilidad y dos falsos positivos del checkout parcial de este worktree.
La tabla usa las mismas cinco columnas que el registro de arriba para que
`tests/test_no_broken_imports.py` la valide igual.

| Module | Archived on | Reason | Disposition | Safe to re-run? |
|--------|-------------|--------|-------------|-----------------|
| `tools/__init__.py` | No archivado — revisado 2026-09-04 | Falso positivo estructural. La alcanzabilidad se calcula sobre aristas de import punteadas y un `__init__.py` de paquete nunca es el *destino* de un import: se ejecuta implicitamente al importar cualquier hijo, asi que jamas recibe una arista entrante y siempre se ve huerfano. Su cuerpo es un unico docstring: la regla de que nada bajo `tools/` puede importar `config.settings` ni `config.database`. | **Se queda donde esta.** Es el marcador de paquete sin el cual no resuelven ni `python -m tools.audit` ni los `from tools.audit... import` que hacen los tests. Archivarlo tumbaria la suite completa. | **Si — no ejecuta nada.** Declara el paquete y nada mas: sin DB, sin red, sin disco. |
| `tools/audit/__init__.py` | No archivado — revisado 2026-09-04 | El mismo falso positivo estructural que `tools/__init__.py`. Ademas no esta vacio: reexporta `AuditRun`, `Category`, `Evidence`, `Finding`, `Severity` y `redact` para que los checkers importen desde `tools.audit` sin conocer la estructura interna de modulos. | **Se queda donde esta.** Es la superficie publica del auditor; moverlo obligaria a reescribir todos los imports de `tools/audit/checks/*.py` y de los tests del auditor. | **Si — solo reexporta.** No corre logica propia al importarse. |
| `scripts/update_upcoming_matches.py` | No archivado — revisado 2026-09-04 | Falso positivo del checkout parcial. Este worktree solo materializa `weekly.yml` de los nueve workflows y no trae `scripts/orchestrator.py`, que es quien lo alcanza. En master el orchestrator lo importa dos veces (`from scripts.update_upcoming_matches import update_all`, lineas 253 y 304) y los workflows morning, closing, evening y weekly lo ejecutan a diario. | **Se queda donde esta.** Es la ruta de fetch de odds de produccion: archivarla apagaria el pipeline entero. | **No — gasta creditos de API.** Cada corrida consume creditos de The Odds API y hace upsert en `upcoming_matches`; correrlo fuera de su cron acumula las duplicadas stale que esa tabla ya arrastra. |
| `scripts/watchdog.py` | No archivado — revisado 2026-09-04 | El mismo falso positivo del checkout parcial. Su entry point es `.github/workflows/watchdog.yml:37` (`run: python scripts/watchdog.py`, cron `17 0,6,12,18 * * *`), y ese workflow no esta materializado en este worktree. | **Se queda donde esta.** Es el chequeo de salud que avisa cuando el pipeline deja de correr; sin el, una ausencia del cron pasa desapercibida. | **Si — es read-only.** Consulta la DB y manda un mensaje a Telegram solo si detecta ausencia del pipeline. El silencio significa salud: si todo esta bien no emite nada. |

## Como se agrega una entrada

1. Mover el archivo con `git mv <ruta> archive/<ruta>` — **mover, no reescribir**.
   El diff tiene que salir como rename puro; si aparece contenido cambiado, la
   disposicion deja de ser auditable.
2. Agregar una fila a la tabla con las cinco columnas llenas. Ninguna puede
   quedar vacia: una disposicion sin motivo es un borrado disfrazado.
3. La columna **Safe to re-run?** exige una respuesta explicita, y
   *"one-shot ya aplicado"* y *"nunca corrio"* son respuestas distintas:
   - **one-shot ya aplicado** — el script corrio, escribio en produccion y
     volver a correrlo duplicaria datos o gastaria creditos.
   - **nunca corrio** — fallo antes de tocar nada; no hay estado que revertir.
   - **si** — es idempotente y volver a correrlo es inofensivo.
4. `tests/test_no_broken_imports.py` verifica que todo `.py` bajo `archive/`
   tenga su fila, asi que un archivo depositado aqui sin registrar rompe la
   suite.
